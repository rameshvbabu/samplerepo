name: Cleanup Old Branches

on:
  schedule:
    - cron: "*/10 * * * *"   # runs daily at 02:00 UTC
  workflow_dispatch:

jobs:
  cleanup:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Delete old branches
        env:
          EXCEPTIONS: "main dev qa prod develop staging production"
          DAYS: 60
        run: |

          git fetch --prune

          cutoff=$(date -d "$DAYS days ago" +%s)

          for branch in $(git for-each-ref --format='%(refname:short)' refs/remotes/origin/); do
            
            branch_name=${branch#origin/}

            # Skip exceptions
            if [[ " $EXCEPTIONS " =~ " $branch_name " ]]; then
              echo "Skipping protected branch: $branch_name"
              continue
            fi

            # Skip HEAD
            if [[ "$branch_name" == "HEAD" ]]; then
              continue
            fi

            last_commit=$(git log -1 --format=%ct origin/$branch_name)

            if [[ $last_commit -lt $cutoff ]]; then
              echo "Deleting old branch: $branch_name"
              git push origin --delete $branch_name
            fi

          done
